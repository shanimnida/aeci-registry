services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: aeci
      POSTGRES_USER: aeci
      POSTGRES_PASSWORD: aeci
    ports:
      - "5432:5432"
    volumes:
      - aeci_pgdata:/var/lib/postgresql/data

volumes:
  aeci_pgdata:
