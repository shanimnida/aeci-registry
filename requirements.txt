Django>=5.0,<6.0
psycopg[binary]>=3.1
django-environ>=0.11
django-simple-history>=3.5
django-storages>=1.14
# AEGIS admin theme. Pinned exactly: 0.104.1 is built against Django>=5.2
# (we run 5.2.17) and requires Python>=3.12 (we run 3.12.3) — a loose range
# risks a future release drifting off either floor.
django-unfold==0.104.1
boto3>=1.34
Pillow>=10.2
# Reads/writes the .xlsx member profiling import (imports/spreadsheet.py).
# Pinned exactly, matching the version this app was built and tested
# against -- a loose range risks a future release changing cell-type
# handling (imports/spreadsheet.py's _normalize_cell leans on today's
# behaviour for dates, bools and numbers) out from under the import path.
openpyxl==3.1.5
gunicorn>=21.2
whitenoise>=6.6
pytest>=8.0
pytest-django>=4.8
