# AEGIS — build handover record

Every decision taken on the church's behalf while building AEGIS, with what it costs if the
decision was wrong. Read the `Ruling:` lines first — those are the judgement calls.

Branch: build/core-registry · 42 commits · 193 tests passing

---

# SDD ledger — plan: docs/superpowers/plans/2026-08-16-aeci-core-registry.md

Spec: docs/superpowers/specs/2026-08-16-aeci-people-committees-design.md (read, reachable)
Branch: build/core-registry
Environment: Python 3.12.3, PostgreSQL 17.11 installed locally (service postgresql-x64-17),
role `aeci` with CREATEDB, database `aeci`. DATABASE_URL=postgres://aeci:aeci@localhost:5432/aeci
No Docker on this machine.

## Pre-flight conflict scan

### Cross-task pairs (shared file or interface)

| Pair | Produced → Consumed | Finding |
| --- | --- | --- |
| T2 → T10 | `next_member_no() -> str` → imported by `assign_member_numbers` | agrees |
| T2 → T3,T4,T6,T7 | `TimeStampedModel` abstract base | agrees |
| T3 → T4 | `Person` → `HouseholdMember.person`, same file appended | agrees |
| T3 → T10 | `MembershipStatus`, `Person.member_no` → admin action filter | agrees |
| T3 → T12 | `Person.CONTACT_FIELDS`, `status_changed_at`, `MembershipStatus` | agrees — but see R1 |
| T3 → T9 | Person permissions codenames | agrees |
| T5 → T7 | `Committee.is_self_selectable`, `CommitteeFunction` | agrees |
| T6 → T7 | `Appointment.objects.active()`, `Position.code` | agrees |
| T7 → T11 | `CommitteeMembership.objects.active()`, `CommitteeRole` | agrees |
| T8 → T10 | `AccessLog.record(user, person, report, ip)`, `FormScan` | agrees (signature aligned during plan self-review) |
| T9 → T11 | `core.groups` constants | agrees |
| T1 → T8 → T13 | `aeci/settings.py` appended three times; T8 and T13 use `env`, `BASE_DIR`, `DEBUG` from T1 | agrees — order is T1, T8, T13 |
| T4,T7,T8 → T9 | migration filenames referenced as dependencies | **conflict — see R3** |
| T10 → T11 | `PersonAdmin` defined then extended in the same file | agrees |
| T5,T6 → T14 | `Committee.code`, `CommitteeMembership` role values | agrees |

### Per-task internal agreement (tests vs code the task itself specifies)

| Task | Finding |
| --- | --- |
| T1 | agrees |
| T2 | agrees |
| T3 | **conflict — `test_status_change_stamps_the_time` contradicts the `save()` it specifies. See R1** |
| T4 | agrees |
| T5 | agrees |
| T6 | agrees |
| T7 | **conflict — `test_leaving_a_committee_frees_a_slot` cannot pass against the `active()` it specifies. See R2** |
| T8 | agrees |
| T9 | agrees (given R3) |
| T10 | agrees |
| T11 | agrees |
| T12 | agrees |
| T13 | agrees |
| T14 | agrees |

## Rulings

Ruling: R1 — T3's `test_status_change_stamps_the_time` asserts `status_changed_at is None`
immediately after `Person.objects.create()`, but the `save()` the same task specifies stamps
on creation, because `_loaded_status` is unset for a new object. Keeping the code and amending
the test. Why: spec §5.1's retention job filters `status_changed_at__lt=cutoff`, and NULL never
satisfies that comparison — so a person encoded directly as TRANSFERRED or DECEASED would never
be purged. Stamping on creation is the correct behaviour and the test is what is wrong.
Cost if wrong: `status_changed_at` reads as "record created" rather than "status last changed"
for never-changed records; the retention window would start at encoding time rather than at the
real transfer date, purging contact details slightly early.

Ruling: R2 — T7's `test_leaving_a_committee_frees_a_slot` sets `date_left = TODAY`, but the
`active()` queryset the same task specifies treats `date_left >= today` as still active, so the
slot is not freed and the third join raises. Amending the test to use past dates
(`date_joined = TODAY - 7 days`, `date_left = TODAY - 1 day`) rather than changing `active()`.
Why: a membership ending today should still count as active today — that is the correct
semantic, and changing `active()` to exclude it would also silently change §7's future reports.
Cost if wrong: none to production code; the test would merely be testing a slightly different
day boundary than intended.

Ruling: R3 — T9's migration `dependencies` name migration files the plan could not know
(`people.0002_household_householdmember`, `committees.0004_committeemembership`). The T9
implementer derives the real names by listing each app's `migrations/` directory before writing
the migration. Why: migration filenames are generated by `makemigrations` and cannot be
predicted. Cost if wrong: `migrate` fails immediately with a clear dependency error, caught by
the task's own test step.

Ruling: R4 — the plan's `docker-compose.yml` targets postgres:16, but this machine has no Docker
and runs PostgreSQL 17.11 natively. Creating the file as specified but not using it; local
development and tests use the installed server. Why: the file costs nothing, helps a future
contributor who does have Docker, and the version difference is immaterial to anything in this
plan. Cost if wrong: a contributor using Docker gets 16 while production gets whatever the host
provides — no behaviour in this plan depends on the difference.

Ruling: R5 — `.superpowers/` was not git-ignored in this repo, though the workspace script
assumes it is. Added to `.gitignore` and committed before any task ran. Cost if wrong: none.

## Deferred minors

Task 1: minor (deferred): `conftest.py` defines an `ict_user` fixture that no test in this plan
uses. Left as the plan specifies rather than diverging; the final review should decide whether
to drop it or whether Plan 2 will use it.

## Progress

Task 1: complete (commits 4844287..dc54f13, review clean)
Task 1: reviewer raised "cannot verify: PostgreSQL 17 native vs postgres:16 image". Resolved by
  controller under R4 — nothing in this plan uses version-specific features or extensions.
Task 2: complete (commits dc54f13..3e71f85, review clean)
Task 2: minor (deferred): no threaded/concurrent test exercises the select_for_update lock; the
  reviewer verified the lock is real by interleaving analysis, but nothing proves it in CI.
Task 2: minor (deferred): get_or_create + select_for_update costs one extra query per allocation.
Task 2: reviewer "cannot verify" items resolved by controller — migrations/__init__.py present in
  all four apps (listed), and AUTH_USER_MODEL is deliberately unset so it resolves to auth.User.

Ruling: R6 — the replacement test issued under R1 asserted `status_changed_at > first_stamp`
across two save() calls, which is timing-dependent: the implementer measured ~1ms clock
resolution on this machine and observed 1 spurious failure in 5 runs. Rejecting `>=` (it would
pass even if the re-stamp never happened, making the test vacuous) in favour of pushing the
first stamp a day into the past with a queryset .update() before the status change. Why: a
flaky test in a church register's suite is worse than no test — it teaches whoever inherits this
to re-run until green. Cost if wrong: the test no longer proves the two stamps are distinct
within a single run, only that a status change moves the stamp forward from a known past value.
Task 3: pre-review correction under R6 (implementer reported DONE_WITH_CONCERNS: flaky test).
  Fix commit 8957720; test file re-run 5x consecutively, 7/7 each time. Model code untouched.
Task 3: minor (deferred): save() is not update_fields-aware — recomputes has_missing_data and
  status_changed_at in memory but does not persist them when update_fields omits them.
  Not load-bearing here: T12's purge and T10's member_no assignment both pass update_fields, and
  in both cases the un-persisted recomputation happens to be the behaviour we want.
Task 3: minor (deferred): from_db eagerly reads membership_status, defeating .only()/.defer().

Ruling: R7 — review found _loaded_status goes stale after refresh_from_db(), because
refresh_from_db copies concrete field values from a separate instance and never calls from_db on
self. Real, and plan-mandated code, so ruled on before dispatching a fix: the plan's code is
incomplete rather than contradicted, and adding a refresh_from_db override fights nothing the
plan requires. Fixing rather than deferring, because Important findings enter the loop by rule
and because Person is the model every later task builds on. Cost if wrong: a few lines of
override on a model that no current task exercises through this path — dead weight at worst.
Task 3: fix round 1/5 (1 addressed, 0 open — stale _loaded_status after refresh_from_db;
  commits 8957720..1e56d91)
Task 3: minor (deferred, CARRIED INTO TASK 4): the refresh_from_db override omits Django 5.2's
  from_queryset parameter, so arefresh_from_db() raises TypeError. Re-reviewer confirmed with a
  live repro against the installed Django 5.2.17. Dormant — no async code in the repo today.
Ruling: R8 — carrying that one-line signature fix into Task 4's dispatch rather than parking it
  or spending a fix round on it, because Task 4 opens people/models.py anyway. Why: a Minor
  finding does not extend the loop by rule, but leaving a proven TypeError in the foundation
  model to be rediscovered later costs more than one line in the next dispatch.
  Cost if wrong: the fix rides in a commit labelled for household models rather than its own.
Task 3: complete (commits 3e71f85..1e56d91, review clean)
Task 4: complete (commits 1e56d91..f3c0212, review clean, no findings). Carried fix R8 landed and
  verified live: arefresh_from_db() no longer raises.
Ruling: R9 — Task 5 review raised Important: unseed() deletes committees by code, and if Task 7's
  CommitteeMembership.committee were CASCADE, reversing the seed on a populated database would
  silently destroy real member-to-committee assignments. Checked the plan: Task 7 already
  specifies on_delete=models.PROTECT on that FK, so the reverse raises ProtectedError instead —
  the loud failure the reviewer wanted. No fix dispatched; instead carrying an explicit
  verification of that on_delete value into Task 7's dispatch and its review prompt.
  Cost if wrong: if Task 7 lands CASCADE unnoticed, a migration reversal on production data
  destroys committee rosters silently. Mitigated by making it a named check in Task 7's review.
Task 5: complete (commits f3c0212..e2e3634, review clean — 12 committees seeded, all slugs and
  sub-functions verified byte-identical to the Board minutes; Grievance sole non-self-selectable)
Task 5: reviewer "cannot verify: committees in INSTALLED_APPS" resolved by controller — Task 1
  wrote all four apps into INSTALLED_APPS and its review confirmed them.
Ruling: R10 — Task 6 review raised Important: clean() is not called by save(), so
  Appointment.objects.create() / bulk_create() can write an overlapping appointment silently;
  only ModelForm paths (the admin) call full_clean(). Real, but it is the spec's explicit
  design — spec §3.3 mandates validation in clean() "so they surface as ordinary admin form
  errors" — and Phase A ships admin-only, so the admin is the only human write path. Declining
  a save() override (it would break the fixture setup several tasks rely on and Django's own
  loaddata/migration paths) and declining DB exclusion constraints (needs btree_gist + daterange,
  disproportionate here). Mitigations instead: (a) Task 14's seed_officers command must call
  full_clean() before saving, carried into its dispatch; (b) flagged for the final review and for
  Plan 2, which introduces non-admin write paths. Cost if wrong: a script or shell session that
  forgets full_clean() can seed an overlapping officer term or a third committee membership, with
  no error until someone reads the data.
Task 6: minor (deferred): committees app has two migrations numbered 0003
  (0003_position_historicalappointment_appointment and 0003_seed_positions). Django orders them
  correctly by explicit dependency and makemigrations --check stays clean; it is confusing only
  to a human listing the directory. Not renaming — the seed migration is already applied, so a
  rename would leave a stale django_migrations row and re-run the (idempotent) seed.
Task 6: complete (commits e2e3634..7abe74e, review clean — overlap rule independently verified
  across 7 interleavings, none wrongly rejected or allowed)
Ruling: R12 — Task 7 review found an Important live defect in the plan's own model code:
  _check_self_selected_limit and _check_single_chairperson guard on `self.date_left` truthiness,
  which treats a FUTURE departure date as "already left", while active() counts date_left >= today
  as still active. Result: a membership created with date_left = today+30 skips both checks,
  passes full_clean(), and is active for 30 days — three self-selected committees, or two
  simultaneous chairpersons. Reachable through the normal admin form path, so unlike R10 this is
  not covered by the "validation lives in clean()" design. Fixing: a _has_ended() helper meaning
  `date_left is not None and date_left < timezone.localdate()`, used by both guards, plus two
  covering tests demonstrated red before green. Cost if wrong: none identified — the helper makes
  the guards agree with active(), which is the invariant the rules were always meant to express.
Task 7: fix round 1/5 (1 addressed, 0 open — future date_left bypass; commits a79314a..2425550)
Task 7: complete (commits 7abe74e..2425550, review clean — four rules independently verified,
  committee FK confirmed PROTECT in both model and migration per R9)
Ruling: R13 — Task 8 review raised three Important findings, all in the plan's own code. Fixing
  all three. (a) AccessLog.person was CASCADE, so the audit trail deletes itself with its subject;
  PROTECT would block ICT's one legitimate hard delete (duplicate rows), so SET_NULL — the entry
  survives with user, timestamp and IP. (b) AccessLog.record() accepted an entry with neither
  person nor report, or both; now raises ValueError unless exactly one is given. (c) STORAGES was
  defined only inside the USE_S3_STORAGE branch, so with the flag off Django's default STORAGES
  silently replaced whitenoise's CompressedManifestStaticFilesStorage — a production deploy
  without S3 would lose compression and manifest hashing with no error. Static and media are now
  decoupled: STORAGES is defined unconditionally with whitenoise for staticfiles, and only the
  "default" entry is swapped when S3 is on. This is the one most likely to bite in practice, since
  the church's first deploy will probably run before object storage is configured.
  Cost if wrong: (a) an orphaned log row naming no person after a hard delete — still better than
  no row; (b) a caller that legitimately wanted both fields must now pick one; (c) none.
Task 8: minor (bundled into the same round, not a separate loop):
  test_a_report_writes_one_entry_not_one_per_row could not fail — it called record() once and
  asserted one row existed. Renamed to what it actually checks, with a comment pointing at Task 10
  where the real one-entry-per-report guarantee lives. Faking the test would have been worse.
Task 8: fix round 1/5 (4 addressed, 0 open — CASCADE audit trail, unguarded record(), coupled
  storage flag, misleading test; commits 19452c3..d4214cd)
Task 8: complete (commits 2425550..d4214cd, review clean)
Ruling: R14 — ratifying the Task 9 implementer's unprompted deviation. The plan's create_groups
  would have left EVERY group empty on a fresh database: Permission rows are created by the
  post_migrate signal, which fires once after an entire migrate run, so a mid-run
  Permission.objects.filter() matches nothing. The implementer forced create_permissions against
  the historical app registry inside the migration. Review verified this against Django's own
  source: migrate.py runs the whole plan before emit_post_migrate_signal; the app_config
  .models_module sentinel is required (AppConfigStub never sets it, so create_permissions would
  raise AttributeError, not no-op); create_permissions dedupes before bulk_create so re-runs are
  safe; and apps.get_app_configs() on the frozen registry covers every app this migration depends
  on. Correct fix, correctly justified. Cost if wrong: none identified.
Task 9: reviewer flagged "Secretariat source=22 but report says 21 granted" as unverifiable from
  the diff. Controller resolved by querying the database directly: every group matches its source
  list exactly (ICT 68 via __all__, Secretariat 22, Treasurer 0, Board 14, Chairperson 3), with no
  missing and no extra codenames. The 21 was a miscount in the report, not a real gap.
Task 9: minor (deferred): create_permissions(...) omits using=schema_editor.connection.alias, so
  it targets DEFAULT_DB_ALIAS. Harmless on this single-database project; latent if routing is
  ever added.
Task 9: minor (deferred): the Secretariat, Board and ICT tests assert individual codenames with
  in/not in rather than exact sets, so a gap in an untested codename would stay green. The
  Chairperson and Treasurer tests do use exact assertions. Inherited from the plan's own tests.
Task 9: complete (commits d4214cd..39761bf, review clean)
Ruling: R15 — Task 10 review found a Critical defect in the plan's own admin code: change_view
  wrote the AccessLog entry before super() ran Django's permission check, so a refused user still
  left a committed row asserting they viewed that person's record. For a log whose whole purpose
  is answering an RA 10173 complaint, false entries are worse than missing ones. Fixing by gating
  the write on self.has_view_or_change_permission(request, person) — the same check Django is
  about to enforce — rather than moving the write after super(), which would cost a second
  get_object() query. Also fixing Important: AccessLogAdmin forbade add and change but not
  delete, while ICT holds delete_accesslog via "__all__" — an audit log the admin can clear.
  Cost if wrong: the permission predicate could diverge from what super() enforces in a future
  Django version, under-logging rather than over-logging; covered by a test asserting a permitted
  view IS logged.
Task 10: minor (bundled into the same round): HouseholdMemberInline was reused verbatim under
  HouseholdAdmin, where `household` is the excluded parent link, so that screen rendered a plain
  dropdown of every person instead of autocompleting `person`. Given its own inline. Small, but
  it is a screen the Secretariat uses daily.
Task 10: minor (accepted, not fixed): assign_member_numbers issues 3-4 queries per person
  serially with no chunking. Correct under concurrency and idempotent on re-run; a timeout risk
  only past roughly a thousand un-numbered members, which this congregation will not reach.
  Revisit only if the registry is ever reused at a larger scale.
Task 10: minor (accepted, not fixed): change_view logs on both the GET and the POST of one edit
  session, so an edit produces two rows. Completeness beats tidiness in an audit log.
Task 10: reviewer confirmed (asked explicitly) that declaring fieldsets/inlines/readonly_fields
  as class attributes does NOT shadow Task 11's planned get_* overrides. No rework needed.
Task 10: fix round 1/5 (3 addressed, 1 new — permission gate looser than Django's on POST;
  commits 04ac3cb..801a50c)
Ruling: R16 — the round-1 gate used has_view_or_change_permission, which matches Django's GET
  path but not its POST path, where _changeform_view requires change permission only. A user with
  view_person and not change_person (the Chairperson group's exact shape) POSTing to the change
  URL still got a phantom log row. Treating this as Important new breakage and running round 2
  rather than parking it, because it is the same Critical defect surviving on a narrower path and
  is reachable by a real role. Fix mirrors Django's method-dependent check. Covering tests build
  the view-only user from a raw Permission rather than the Chairperson group, so Task 11's
  incoming group-based row scoping cannot break them. Cost if wrong: the mirrored predicate could
  drift from Django's in a future version; the paired "view-only GET IS logged" test guards the
  under-logging direction.
Ruling: R17 — folding the collectstatic fragility into the same round. The admin-rendering tests
  only passed because the implementer had run collectstatic: production uses whitenoise's
  CompressedManifestStaticFilesStorage, which raises without a manifest, so a fresh clone would
  fail the suite for a reason nobody would diagnose quickly. Adding an autouse conftest fixture
  that swaps staticfiles storage for tests only, proven by deleting staticfiles/ and running the
  suite clean. Why fold in rather than defer to Task 11: same file, same round, and Task 11 adds
  more admin-rendering tests that would hit it again. Cost if wrong: tests no longer exercise the
  manifest storage path — acceptable, since that path is a deployment concern, not app behaviour.
Task 10: fix round 2/5 (2 addressed, 0 open — POST permission gap, collectstatic dependency;
  commits 801a50c..52724f4). Re-reviewer independently reverted people/admin.py to 801a50c and
  reproduced the failure rather than trusting the report.
Task 10: minor (deferred): nothing exercises the real whitenoise CompressedManifestStaticFilesStorage
  path now that tests swap it out. That is a deployment-verification concern; Task 13's checklist
  includes `manage.py check --deploy`, and the final review should decide whether a collectstatic
  smoke check belongs in the repo.
Task 10: complete (commits 39761bf..52724f4, review clean, 2 fix rounds)
Ruling: R18 — Task 11 review confirmed a CRITICAL data leak, verified live against the running
  app: django-simple-history's history_view calls self.get_queryset(request).get(pk=object_id),
  and on DoesNotExist — exactly what chairperson row scoping produces — falls back to
  historical_records.latest().instance, the raw unfiltered manager. It then renders a full
  diff_against() over every tracked field. A chairperson could read home_address, date_of_birth,
  civil_status and emergency contacts for ANY person in the church, roster or not, defeating both
  the row-level and field-level scoping this task exists to provide. Closing it by overriding
  history_view and history_form_view to raise PermissionDenied for chairperson-only users, rather
  than trying to scope the history queryset: a chairperson sees five fields, so they have no
  business reading revision history at all, and denial cannot be defeated by a fallback path
  inside a third-party package we do not control. Cost if wrong: chairpersons lose a history view
  they were never intended to have.
Ruling: R19 — same round, three more: (a) list_display rendered member_no, membership_status and
  has_missing_data to chairpersons — rows were scoped, columns were not; adding get_list_display
  and get_list_filter overrides. (b) get_queryset used .active() for "committees I chair" but a
  raw FK join for the roster, so someone who left a chaired committee years ago stayed visible;
  rewritten as an active-membership subquery, which also removes the need for .distinct().
  (c) pre-existing from Task 10: the assign_member_no admin action declared no permissions=, so a
  view-only chairperson could mutate member_no on their roster; adding permissions=["change"].
  Cost if wrong: (a) and (b) narrow what chairpersons see, which is the intended direction;
  (c) could hide the action from a role that legitimately needs it, but only ICT and Secretariat
  hold change_person and both should have it.
Task 11: fix round 1/5 (4 addressed, 0 open — history-view leak, changelist columns, stale roster
  join, unguarded action; commits 6083479..6b144d0). Re-reviewer enumerated every route
  SimpleHistoryAdmin registers and confirmed both are now shadowed with matching signatures.
Ruling: R20 — the round-1 re-review surfaced, in its deferred section, that
  CommitteeMembershipAdmin has no chairperson scoping at all: the Chairperson group holds
  view_committeemembership, so a chairperson could browse every membership in the church — names,
  member numbers, committees, roles — plus its history view, the same route just closed on
  PersonAdmin. The skill's rule is that out-of-scope observations do not extend the loop, and I am
  overriding that deliberately for this one: it is the same defect class as the Critical just
  fixed, in the area Task 11 conceptually owns, and closing it now is cheaper than a final-review
  fix wave. Also moving is_chairperson_only from people/admin.py to core/groups.py so both admins
  share one predicate rather than committees.admin importing people.admin. Cost if wrong: one
  extra fix round, and a predicate relocated to a module that already exists for exactly that
  purpose.
Task 11: fix round 2/5 (1 addressed, 0 open — CommitteeMembershipAdmin scoping; commits
  6b144d0..270c262). AppointmentAdmin confirmed already unreachable for chairpersons (no
  view_appointment permission), verified empirically rather than assumed.
Task 11: complete (commits 52724f4..270c262, review clean, 2 fix rounds)
Task 12: implemented (commit 131d600, 69 tests). Review dispatched as a four-lens adversarial
  workflow (spec / privacy / unattended-operation / test-quality), each non-minor finding put to
  one refuter and one reproducer before it counts. Ultracode was enabled at this point in the
  session, so review rigor was raised for the remaining tasks and the final branch audit.

SCOPE ADDITION (user request, 2026-08-17): add a deployment runbook as a task after Task 14.
  A file in the repo walking GitHub -> Neon -> Render step by step, naming every environment
  variable, written so the next ICT volunteer can redo the deployment without the current one.
  Deployment itself stays the user's to perform: it requires accounts in the church's name and a
  production DATABASE_URL that should not pass through this session. The Procfile already runs
  migrate as a release command, and the first superuser is created through the host's own shell.
  Real go-live gate is non-technical: no member data until the RA 10173 consent clause is signed
  on 23 August 2026.
Task 12: four-lens adversarial review returned 29 findings, 34 verifier votes (16 upheld).
  NOTE: the workflow's own aggregation returned empty confirmed/refuted arrays — a bug in my
  post-processing stage, not in the agents. Findings were recovered from journal.jsonl. If this
  workflow shape is reused, fix the stage-2 return before trusting its summary.
Ruling: R21 — CRITICAL, found independently by three of the four lenses and missed by every
  single-reviewer pass before it: the retention purge does not purge. Person carries
  HistoricalRecords(), so every edit writes a full copy of home_address, mobile_number, email and
  the emergency-contact fields to people_historicalperson, which the command never touches and
  the admin history page renders indefinitely. Spec §5.1's policy is therefore not delivered at
  all. This is a plan defect of the worst kind: Task 3 added change history and Task 12 wrote a
  retention job, each correct alone, cancelling each other out. Fixing by scrubbing all
  CONTACT_FIELDS across every historical row — using the full field list rather than only those
  non-empty on the live row, since history can hold values the live row has since lost, and
  restructuring the loop's skip condition, which would otherwise permanently skip anyone whose
  live row is already blank. Cost if wrong: history rows lose contact values that were never
  meant to be retained past two years — which is the point.
Ruling: R22 — Important, same round: (a) spec §5.1 requires each purge be written to the audit
  log and nothing did; adding a records.PurgeRecord model with a text snapshot of the person's
  identity so the record survives independently, registered readonly and undeletable like
  AccessLog. Note this becomes the ONLY evidence a purge occurred once R21 lands. (b) the same
  home address survived on Household.address; clearing it only when every member of that
  household is purgeable and past the window, since a household is shared and one member leaving
  must not erase the family's address. (c) the tests set only 4 of 6 CONTACT_FIELDS, never
  exercised DECEASED, and tested the 730-day boundary at 800 and 100 days — so "cleared" was
  indistinguishable from "never filled in" and half the purge trigger was unprotected.
Task 12: minor (accepted, not fixed): updated_at not bumped under update_fields; free-text notes
  and follow_up_notes may hold phone numbers (a data-entry policy question for the church, not a
  code fix — worth raising with the Secretariat); linked User.email survives (Phase A issues no
  member logins); purge loop is neither atomic nor locking; handle() returning a string makes
  Django echo a bare count line.
Task 12: fix round 1/5 (4 addressed, 0 open — history not purged, no audit record, household
  address survived, six test defects; commits 131d600..7acd70c). Re-reviewer independently
  reverted the command and reproduced the red run. Spec §5.1 corrected in the same commit.
Task 12: minor (deferred): PurgeRecord.fields_cleared always lists the full CONTACT_FIELDS set
  even when only history was dirty — imprecise, not incorrect.
Task 12: complete (commits 270c262..7acd70c, review clean, 1 fix round, 78 tests)
Task 13: implemented (commit 12196b8, 81 tests). Restore drill genuinely performed against a
  scratch database, counts matched, scratch dropped. pg_dump path made a setting rather than
  hardcoded; same-minute collision guard added beyond the brief; /backups/ and *.sql gitignored.
  Review dispatched as a four-lens workflow. Controller's own prior suspicion, written down
  before the result arrives so it cannot be retrofitted: the command appears to write dumps to a
  local directory, and spec §8.4 requires them in OBJECT STORAGE. On the intended free-tier host
  the filesystem resets on redeploy and idle spin-down — the exact reasoning behind D11 for scan
  images. If that holds, the church would believe it had nightly backups and have none, which is
  worse than knowing it has none.
Ruling: R23 — Task 13 review returned FAIL from all four lenses, confirming the suspicion recorded
  before the result arrived. CRITICAL: the backup command writes a local file and never touches a
  storage backend, though spec §8.4, the plan's file table and the brief's own Interfaces line all
  say it must. On the intended free-tier host the disk resets on redeploy and idle spin-down —
  the same mechanism D11 was written about for scan images — so the church would believe it had
  nightly backups and have none. Second CRITICAL found by reproduction, not inspection: killing
  pg_dump mid-run leaves a truncated file at the FINAL backup name, matching the glob the test
  uses to assert success, and the same-minute guard then refuses the retry, leaving the corrupt
  stub as the newest artifact. Fixing both, plus gzip (7.9x measured), 30-day retention that never
  prunes the last backup, sslmode passthrough, restrictive file permissions, and RESTORE.md gaps
  (the drill leaves a plaintext copy of the whole register with no instruction to destroy it, and
  there is no documented path from "the hosted database is gone" back to a working register).
  Scheduling and the §8.4 admin export are explicitly out of scope for this round: free-tier hosts
  have no usable cron, so scheduling belongs in the deployment runbook, and the export belongs to
  Plan 2's reporting layer. Cost if wrong: the command grows a storage dependency it exercises
  only when configured, and local development behaviour is unchanged.
Task 14: implemented (commit 203bacf, 89 tests) in parallel with Task 13's review — disjoint file
  sets, and Task 13's implementer was idle at dispatch time, so no git index contention.
  R10 honoured: seed_officers calls full_clean() on every Person and CommitteeMembership, reports
  per-row violations with the CSV row named, rolls the row back on a savepoint, and exits non-zero
  if any row failed. data/officers.csv committed rather than gitignored — governance facts from
  Board minutes, unlike the members' personal data in data/scanned-forms/.
Ruling: R24 — Task 14 review: R10 validation confirmed airtight on both models and both branches.
  Two Important findings, both about the CSV's life after its first run rather than its first run.
  (a) The file is opened as utf-8, so Excel's "CSV UTF-8" export — the realistic output of the
  very workflow this command serves — prefixes a BOM and the command reports "missing column:
  last_name" while that column is present. Fixing with utf-8-sig. (b) Changing an officer's
  committee_code creates a SECOND active membership without closing the first, showing someone as
  active Chairperson of two committees. Ruling explicitly AGAINST auto-closing the old membership:
  a spreadsheet edit is not a reliable instruction to end someone's service, and this command must
  never silently mutate service history. Instead it refuses the row, names both committees, and
  tells the operator to end the existing membership in the admin. (c) Minor upgraded to worth
  fixing: nickname corrections were dropped on re-run, which defeats the file's entire purpose —
  it exists because officers are recorded by nickname and resolved to full names over time.
  Cost if wrong: an officer genuinely moving committees needs one manual admin action before the
  CSV import will accept the change.
Task 14: fix round 1/5 (3 addressed — BOM, moved-officer duplication, dropped nickname updates;
  commit 804bd80, 101 tests)
Ruling: R25 — correcting my own R24 instruction. The moved-officer refusal I specified applied to
  ANY role, but two MEMBER memberships on self-selectable committees is exactly what the
  two-committee cap permits, and CommitteeMembership.clean() already enforces it. The implementer
  flagged the over-strictness rather than implementing it silently. Narrowing the refusal to
  CHAIRPERSON and CO_CHAIR, where two simultaneous holdings are the anomaly worth stopping a human
  for, and letting MEMBER and OVERSIGHT fall through to the model, which is the authority on
  validity. Cost if wrong: a second simultaneous co-chair role needs one manual admin action.
Task 14: fix round 2/5 (1 addressed — refusal narrowed to CHAIRPERSON/CO_CHAIR; commit 9151b9c)
Task 14: parked — Person lookup in seed_officers keys only on (last_name, first_name), and the
  new nickname-update path gives that ambiguity a silent WRITE consequence: two same-named people
  could have one's nickname overwritten by the other's CSV row. Ruling: parking rather than fixing.
  The system deliberately allows shared names (Task 10's duplicate detection warns, never blocks),
  so a unique key would have to come from the church, not be invented here; and the command's
  scope is a handful of officers the ICT Committee edits by hand. Pointing the final whole-branch
  review at it. Cost if wrong: a nickname silently lands on the wrong record among same-named
  officers — visible and correctable in the admin, not data loss.
Task 14: complete (commits 12196b8..9151b9c, review clean, 2 fix rounds, 103 tests)
Task 13: fix round 1/5 (7 points addressed; commit d0c9eb4, 104 tests). Gzipped restore drill
  genuinely performed, never writing a plaintext copy of the register to disk. Implementer
  self-caught and reported two bugs it introduced mid-fix: an icacls grant using (OI)(CI)
  inheritance flags on a FILE produced an empty ACL, making the backup unreadable by everyone
  including the owner (caught by its own test suite); and an sslmode test mutating
  settings.DATABASES in place leaked sslmode=require into later tests, which pytest-django does
  not revert. It also extended the partial-write pattern to the gzip step unprompted, on the
  reasoning that a failure there would reproduce the same corrupt-file defect one stage later.
  Scoped re-review dispatched to verify all three independently.
Task 15 (deployment runbook, user-requested scope addition) dispatched in parallel — documentation
  only, disjoint from the re-review's files.
Task 13: minor (deferred): a hard kill of the whole process between the .sql rename and the .sql.gz
  rename skips the finally block, leaving an orphan plaintext .sql that the guard and retention
  globs (.sql.gz only) never see. Permissions are inherited from the hardened directory so it is
  not an exposure gap, just an untracked file.
Task 13: minor (deferred): storage-side pruning runs only when the current run uploads, so turning
  BACKUP_TO_STORAGE off later leaves previously-uploaded backups unpruned forever.
Task 13: complete (commits 7acd70c..d0c9eb4, review clean, 1 fix round, 104 tests)

=== FINAL WHOLE-BRANCH AUDIT (6 dimensions, all returned FIX_FIRST) ===
Workflow wn68s8wx8 produced 7 Critical, ~35 Important, ~30 Minor across integrity, privacy,
coherence, operations, tests and simplification. Its final aggregation was LOST (0-byte output);
findings recovered from journal.jsonl. 27 verifier votes ran, 17 upheld, but the vote-to-finding
mapping was lost with the aggregation, so findings are being triaged on their merits by the
controller rather than on verifier votes. If this workflow shape is reused, fix the aggregation
stage first — this is the second time it has failed the same way.

CRITICALS (all being fixed):
 C1 ControlNumberSequence unique constraint uses a NULL year, and Postgres treats NULLs as
    distinct — so the constraint does not constrain and duplicate MEM- numbers are possible.
 C2 Chairperson field restriction bypassable via changelist query-string filters (no
    lookup_allowed override on PersonAdmin).
 C3 Scanned profiling form images sit outside the retention purge — the R21 shape again, one
    storage layer out: the database is scrubbed and a photograph of the whole form remains.
 C4 No LOGGING configuration at all; every production traceback is discarded.
 C5 The five-field chairperson rule is tested through a hook Django never uses to render the
    form, so the test proves nothing about what a chairperson actually sees.
 C6 Both halves of the R21 retention fix survive reversion — the suite cannot tell fix from bug.
 C7 The scheduled backup workflow in DEPLOYMENT.md installs a pg_dump older than Neon's server,
    which refuses to dump it.

Controller scope ruling for the unattended window: all 7 Criticals, the Importants that are real
defects rather than documentation drift, one batched Minor cleanup, and corrections to the
deployment docs. Explicitly NOT attempting all ~70 in the time available; the remainder is
reported to the user as a written list rather than silently dropped.
C1: fixed (commit 431928f). Red run proven: the old constraint DID NOT RAISE IntegrityError on a
  duplicate ('MEM', NULL) row, so the uniqueness guarantee never existed.
Ruling: R26 — the C1 agent exposed reconcile_member_sequence() only as a management command and
  deliberately did NOT wire it into next_member_no(), because doing so broke
  tests/people/test_admin_encoding.py::test_member_numbers_are_assigned_in_sequence_and_never_overwritten.
  That test asserts a fresh member gets MEM-0001 while an existing record already holds MEM-0099.
  That test encodes the BUG as the expected behaviour — it is a test I wrote in the plan, and it
  is wrong. The real day-one failure is exactly this: the Secretariat types handwritten numbers
  off paper, then uses "Assign member numbers", and the allocator hands out numbers that collide
  with or leave permanent gaps around what is already written. Leaving that to a command a
  volunteer must remember to run is not a fix. Ruling: correct the test to expect MEM-0100, and
  wire reconciliation into the assignment path so collisions are impossible by construction.
  Deferred only because people/admin.py and tests/people/test_admin_encoding.py are held by the
  concurrent re-skin agent; queued for the next wave. Cost if wrong: reconciliation runs one extra
  cheap query per assignment batch.
C3+C6: fixed (commit f8e613d). Scan image file deleted, FormScan row kept with form_type,
  uploaded_by, uploaded_at and person intact — the register still records that a form existed and
  was encoded, while the personal data in the image is gone. PurgeRecord.scan_files_removed
  records the count. Root cause of the weak retention tests diagnosed precisely: every existing
  test left the live row dirty, so person.save()'s side effect — a fresh, already-blank history
  row — masked a disabled _scrub_history. New test blanks the live row first, reproducing the
  original bug's exact shape, so _scrub_history is the only thing that can clear history.
  Red runs proven for BOTH defects: 3 failed / 16 passed with each fix commented out.
Re-skin: done (commit 763bc1b). django-unfold 0.104.1 pinned. Chose it over jazzmin (dated
  Bootstrap) and admin-interface (no real sidebar, still reads as stock Django). Composes with
  SimpleHistoryAdmin because every security-sensitive method is defined directly on PersonAdmin /
  CommitteeMembershipAdmin rather than inherited. Ships prebuilt static assets, so no npm build
  risk on Render. Chairperson restriction verified through the RENDERED page, not the hooks:
  rendered fields = the five permitted, inline formsets = [], history = 403. Zero "Django"
  strings in the login page or admin index. 118 tests passing.
Ruling: R27 — pushing the re-skin to the branch now rather than batching with the remaining fixes.
  The branch still carries the unfixed C2 chairperson filter bypass, so this deploys known-broken
  access control. Accepting that because the production database holds no member data at all —
  the user was told explicitly not to encode anyone until consent is signed on 23 August — so the
  bypass has nothing to disclose, and the user wakes to a system that looks like the product they
  named instead of the stock admin they said they would not use. Cost if wrong: if anyone encodes
  a real person before the C2 fix deploys, a chairperson account could infer withheld fields via
  URL filters. No chairperson accounts exist yet either.
Committee gaps: fixed (commit c5c10d8, 123 tests). Date-ordering check added — only
  date_left < date_joined is rejected; a same-day join-and-leave stays legal, on the reasoning
  that active() already counts a membership ending today as active today, so zero-duration is the
  coherent extension of that rule rather than the typo the check exists to catch.
Ruling: R28 — seeded officer status resolved well and better than my steer. membership_status is
  now a REQUIRED CSV column the ICT Committee fills in, applied only when creating a brand-new
  Person and never touching an existing one on re-run. This uses the code's own create-vs-change
  rule (a new instance has no _loaded_status, so no approved_by is demanded) instead of either
  defaulting to RELATED, which misdescribes a chairperson, or hardcoding MEMBER, which would be
  the software deciding membership — the one thing the design is emphatic it must never do.
  All six chairpersons set to MEMBER by the committee, not by the code.
Docs: fixed (commit f2abcc0). All nine audit items confirmed REAL, none already-correct, each
  verified against code or installed package source before writing. Notable confirmations:
  Ubuntu 24.04's default postgresql-client is v16 while Neon now provisions v18, and pg_dump
  refuses a newer server — the scheduled backup would have failed from the first night; the
  Content-Encoding gzip problem traced into django-storages' _get_write_parameters and reproduced
  locally with mimetypes.guess_type(); no ON_ERROR_STOP existed anywhere, so every documented
  restore could half-complete and exit 0; and the claimed bucket/credential isolation between
  database dumps and members' scanned forms does not exist — one bucket, one token — now stated
  honestly rather than asserted away.
  Two code-change recommendations left unimplemented and passed to the user: a second R2 bucket
  and token for real backup/scan separation, and a dead-man's-switch ping once a volunteer
  actually creates the scheduled workflow file.
Permissions: fixed (commit d742f05, 34 core tests). ICT set re-evaluated against the current model
  registry so PurgeRecord is no longer invisible; Board granted view_purgerecord and change_person;
  Secretariat's delete_household and delete_householdmember REVOKED — Gap 3 confirmed true, since
  HouseholdMember.household is CASCADE and HouseholdMember is the one model carrying no history,
  so a Secretariat household deletion destroyed rows nothing could reconstruct. Edit rights kept
  so routine corrections still do not need ICT. Forward/reverse/forward round-trip verified on a
  scratch database.
Ruling: R29 — the agent found a real contradiction in MY spec, not in the code: §4's matrix ticks
  ICT for "Set status → MEMBER" while the prose directly beneath asserted "ICT creates logins but
  cannot set membership status." Both cannot be true, and the code implements the matrix. Resolved
  in favour of honesty rather than by tightening ICT: ICT holds the full permission set because it
  is the administrator role, and there is no way to grant the ability to repair a system while
  withholding the ability to misuse it. Rewrote §4 to say so plainly and to state what actually
  constrains ICT — simple-history on every change and AccessLog on every view, both readable by
  the Board. The check on the administrator is visibility, not prohibition. Cost if wrong: none to
  code; the spec now describes what is built instead of what would be reassuring.
Residual (reported, not fixed): Board's change_person is broader than the single matrix cell it was
  granted for, because PersonAdmin has no Board-specific field restriction the way it does for
  Chairperson. A Board member can now edit any field on any person, not only accept them into
  membership. Worth a Phase B field restriction.
Residual (reported, not fixed): the ICT permission grant is correct today but not self-maintaining
  — a model added by a future migration falls through the same crack again. Real fix is a
  post_migrate signal in core/apps.py.

=== IMPORT FEATURE (commit 4671e81, 171 tests) — SECURITY BUG HUNT ===
Verified clean by execution, not inference: chairperson 403 on all four import URLs including a
hand-crafted POST; anonymous 302 to login; Treasurer and Board hold no imports permissions;
CSRF token on every mutating form and no csrf_exempt anywhere; all AI-supplied text rendered
through Django autoescaping with no |safe; source_image never used as a filesystem path.
FINDINGS:
 S1 Important — unbounded upload size. UploadForm.file has no size cap and admin reads the whole
    file into memory before decode and json.loads. A 500MB .json is read entirely into RAM.
 S2 Important — deeply nested JSON crashes with an uncaught RecursionError rather than the
    graceful ImportValidationError the module promises. VERIFIED by calling parse_import_json()
    directly with 10,000 levels of nesting.
 S3 Important — staged PII has no audit trail. people/admin.py writes an AccessLog row on every
    Person view; the import review screen renders the same class of data — name, DOB, address,
    phone, email, children, emergency contact — and never calls AccessLog.record. Already-approved
    and rejected rows stay readable indefinitely, still unlogged. This is a hole in exactly the
    RA 10173 story the rest of the system is careful about.
 S4 Minor/plausible (code shape, not executed) — TOCTOU on approve: no select_for_update or
    status re-check under lock, so two concurrent approve POSTs could both see PENDING and create
    two Person records.

=== IMPORT FEATURE — CORRECTNESS BUG HUNT AND FIXES (commit 25175b1, 193 tests) ===
Verified SAFE by the correctness hunt, no bug: approval atomicity (one transaction, rollback
proven), full_clean() coverage on every created object with no bare objects.create(), and the
membership_status/approved_by interaction on creation.
CRITICALS FOUND AND FIXED:
 C1 The same family in one batch produced duplicate children and split households. Both parents'
    paper forms list the same children — that is how the form works — so approving a husband's row
    then his wife's row created FOUR child records for two real children in two disconnected
    households, with HouseholdRole.SPOUSE never assigned anywhere. This would have broken on the
    very first real import of the user's 22 forms. Red run proved 6 Person rows where 4 were
    expected.
 C2 Double approval created a permanent untraceable duplicate person: the staged row was read
    without a lock, read_only computed from that stale read, the whole person built, and status
    written back only afterward. Red run used real threads against a real Postgres row lock and
    produced two identical Person rows.
Ruling: R30 — on the ambiguous-child-match case the implementer chose a hard refusal
  (ValidationError) over a guess, because the review screen's only resolution path is correcting a
  field and re-submitting, so a partial approval had nowhere to record "unresolved". Ratified:
  refusing is consistent with this feature's whole premise that the human decides and the software
  never guesses about people. Spouse-name ambiguity is treated more leniently (soft skip plus a
  notice) since a wrong spouse link costs convenience, not correctness. Cost if wrong: a reviewer
  meeting an ambiguous child must resolve it by hand before that person can be approved.
Ruling: R31 — date validation placed on Person.clean(), Household.clean() and
  HouseholdMember.clean() rather than in the importer, so every full_clean() write path including
  the admin inlines is protected rather than this one importer. HouseholdMember is the only place a
  household, a role and a specific person are all in hand for the cross-person checks. Scoped to
  refusing the impossible (future birth, future marriage, marriage before birth, child older than
  head) rather than the improbable, so a decades-old mis-keyed birth year on a paper form stays
  encodable. Cost if wrong: a genuinely odd but real family arrangement could be refused and need
  a correction before encoding.
S1-S4 also fixed: upload size cap, RecursionError caught as a graceful validation error, AccessLog
  written on import review, re-upload detected by content hash and warned (not blocked), and the
  duplicate warning moved to before approval instead of after.

=== COMMITTEE CAP: HARD BLOCK TO WARNING (285 tests) ===
Ruling: R32 — CommitteeMembership's "max two self-selected committees" rule (spec §3.3, rule 1)
  changed from a hard refusal to a soft warning. Reality disagreed with the printed form: of the
  first thirty profiling forms collected, four (IMG_5874, IMG_5885, IMG_5893, IMG_5894) ticked
  three or four committees, and the church accepted those forms as filed. "Select up to TWO (2)"
  is church policy printed on a form, not a structural fact about the data the way "a committee
  cannot have two chairpersons at once" is — software refusing to save what the church itself
  already accepted was the software being wrong, not the form. Ratified on the volunteer's own
  words: "this shouldnt be a problem." The one-chairperson-per-committee rule (rule 2) was
  deliberately left untouched — it stays a hard refusal, on purpose, because it is a different
  kind of rule.
  `CommitteeMembership._check_self_selected_limit` (raised ValidationError, called from clean())
  was replaced by `self_selected_overflow_count()` (returns the overflow count or None, never
  raises, not called from clean() at all). The warning now surfaces in two places: committees/
  admin.py's CommitteeMembershipAdmin.save_model, in the same shape as people/admin.py's existing
  duplicate-person warning (find_possible_duplicates); and imports/services.py's new
  committee_cap_warning(data), shown on the import review screen before the reviewer approves,
  same timing as MINOR 8's duplicate warning. Every test that asserted the old refusal was
  rewritten to assert the save succeeds and the warning fires, not deleted — including a
  four-committee case (IMG_5893's shape, fictional data) and a future-departure case proving
  _has_ended()'s guard still keeps a scheduled-but-not-yet-ended membership counted. The seed_
  officers management command's own third-committee test was updated the same way; its docstring
  and help text no longer cite the cap as a business rule the command can fail a row on.
  Docs corrected, not rewritten: spec §3.3, the Global Constraints table, and the Testing table
  now state the rule as a warning and record why it changed; docs/IMPORT_TEMPLATE.md no longer
  tells the AI/volunteer that AEGIS refuses on approval for this. Cost if wrong: a member could
  now accumulate self-selected committees indefinitely with only a message on save to notice it —
  mitigated by the warning firing in both the admin and the import review screen, and by rule 2
  (structural, not policy) staying a hard refusal.

=== EMPTY HOUSEHOLDS: A FAMILY OF ONE IS NOT A FAMILY (401 tests) ===
`build_person_from_import` created a Household for every approved row unconditionally. A member
with no spouse, no children and no marriage date got a "{Surname} Family" household containing
only themselves; the 22 forms waiting to be imported would have manufactured 22 of them.

Ruling: R33 — a household is now created only when the row actually names a family: a spouse name,
  a matched spouse, at least one child, or a date of marriage. The last is not decoration —
  `date_of_marriage` lives on Household and has no home on Person, so a married member whose
  spouse never submitted a form still needs somewhere to keep the date the paper records.
  Implemented as `_ensure_household`, called at each point where something first genuinely needs
  a household rather than up front, so the CRITICAL 1 child- and spouse-linking all still works —
  the children loop calls it before its first real child entry and gets the same household back on
  every later call. Deliberately NOT gated on `civil_status` or any other inference about whether
  someone is married: the trigger is what the form states, never what the software concludes about
  a person. Red run proven — reverting `imports/services.py` alone fails the two tests that assert
  no household, through the service and through the review admin view.
  Cost if wrong: a real family whose form names no spouse, no child and no wedding date is encoded
  with the household left for the Secretariat to add by hand in the admin, which is one screen.

Ruling: R34 — `remove_empty_households` (with `--dry-run`) cleans up the households the bug already
  wrote. Its criteria — exactly one member, that member HEAD, no `date_of_marriage` — do NOT single
  out the bug's output, and this was verified by execution rather than assumed: the FIXED importer
  still produces exactly that shape for a form naming a spouse who is not in the register yet, and
  the command deletes it. Nothing stored on the household distinguishes the two cases, because an
  unmatched spouse's name is never written anywhere.
  Ruled AGAINST three alternatives. Narrowing the importer to drop the bare-spouse-name trigger:
  rejected, it is one of the four triggers the church asked for. Inferring intent from the lone
  member's `civil_status`: rejected, that is the software guessing about a person, which this
  design refuses everywhere else. Silently accepting the overlap: rejected, the command's own help
  would then be claiming a narrower scope than it has, and a volunteer tidying up six months from
  now would quietly undo correct behaviour with no way to know.
  Instead: a `--created-before YYYY-MM-DD` filter on `created_at` so a re-run can be scoped to
  households predating the fix, the creation date printed on every dry-run line so the operator can
  see which are which, and the overlap stated plainly in the command's help text. The boundary is
  local midnight (TIME_ZONE is Asia/Manila), which spares households created on the cutoff date —
  the safe direction, and the help says so.
  The lone member's `home_address` is backfilled from the household's before deletion if their own
  is blank, so no address is lost. No `PurgeRecord` equivalent is written and none is needed: unlike
  the retention purge this destroys no personal data — the address moves rather than disappearing
  and the Person is untouched — so there is nothing for RA 10173 to want evidence of.
  Cost if wrong: a household that should have been kept is deleted; the person survives with their
  address, and the household is one admin screen to recreate. Re-running the command is safe and
  proven idempotent.

Ruling: R35 — found while checking R33's edges, fixed in the same pass: when a row's spouse was
  matched to someone who already had a household, that household was reused (correctly, CRITICAL 1)
  and this row's `date_of_marriage` was dropped on the floor. Both halves of a married couple fill
  in their own form and only one of them may have written the wedding date down, so the datum R33
  exists to protect was being lost on the one path where a household already existed.
  `_reconcile_marriage_date` now fills the household's date when it has none, and reports it to the
  reviewer as a notice. It never overwrites one: two forms disagreeing about a wedding date is a
  question for the reviewer holding both sheets of paper, not something to settle by whichever row
  happened to be approved second, so the stored date stands and the disagreement is reported.
  Note the review form keeps every date as free text (`imports/forms.py`, so an unreadable scrawl
  can still be staged and corrected), which means the incoming value is a string and the stored one
  a `date`; comparing them raw reported a disagreement between two identical dates. Normalised
  through `DateField().to_python` first, with its own test — red run proven by disabling only the
  normalisation. Cost if wrong: a household carries the first-approved form's wedding date and a
  notice tells the reviewer the second form disagreed.

Residual (reported, not fixed): a reused household keeps its original `address` and `name`; a second
  form giving a different address for the same household is neither applied nor reported, the way a
  conflicting marriage date now is. Left alone because an address genuinely changes over time and
  the newer form is not reliably the newer truth — but it is the same shape of silent drop R35 just
  closed, and worth a decision if the Secretariat ever reports a stale household address.

=== CELEBRATIONS: THE SECRETARY'S ORIGINAL REQUEST (453 tests) ===
Spec 7.1, built at the user's request 2026-08-22. Upcoming birthdays and wedding anniversaries as
an admin screen plus a seven-day panel on the landing page. Bounded work: no models, no migration.

Ruling: R36 — named **Celebrations**, not "Events" as first asked. "Event" is already taken twice
  inside AECI: the Events Committee is one of the twelve on the profiling form, and Phase B's EARF
  is the Event/Activity Request Form, where an event is an activity with a budget and an approval
  chain. Spec 7.1's own word collides with neither. Put to the user with the collision explained
  rather than renamed silently. Cost if wrong: none; it is a display string.

Ruling: R37 — the window is a rolling 30 days, not a calendar month. On the 29th a calendar month
  shows almost nothing while three people have birthdays that week. `?days=` widens it, clamped to
  1..366 and falling back to the default on anything unreadable, because a hand-edited URL is not
  worth a 500 on a page the Secretariat opens every Sunday.
  29 February is greeted on 1 March in a common year rather than dropped, and the label says
  1 March, since that is the day the church would actually greet them. Both the year-end rollover
  and the leap-year case are named requirements in spec 9 and are tested in both directions.

Ruling: R38 — an anniversary is dropped when either spouse is excluded, but the exclusion is
  scoped to the household's HEAD and SPOUSE rows rather than to everyone under the roof. Greeting a
  widow on her wedding anniversary is precisely the error spec 7.1 calls embarrassing once and
  preventable permanently; a teenager opting out of their own greetings erasing their parents'
  anniversary is a different thing and would be wrong. A household with a HEAD and no SPOUSE still
  counts — that is R33's case, where the marriage date came from the paper and the spouse never
  submitted a form of their own.

Ruling: R39 — no birth year and no ordinal anywhere, so no "25th anniversary" either. D17 withholds
  the year to keep an age from people who have no need for it, and a wedding ordinal discloses the
  year just as directly. Recorded as a deliberate cost: ordinals are what makes an anniversary
  greeting land, and the church may well want milestones (silver, golden, an 18th debut) later.
  That is one conversation and one line, not something to grant quietly here. A test asserts the
  birth year never reaches the rendered page.

Ruling: R40 — access is `may_view_celebrations`, not this admin's ordinary view permission.
  Spec 7.7 grants this one report to the Sunshine chairperson — greetings and benevolence are that
  committee's actual job — and withholds it from the Board and Treasurer, and no Person permission
  expresses either fact. Note this is deliberately wider than D12 for that one person: Sunshine's
  lead sees names and dates for the whole congregation, not just their own roster. That is the
  spec's call and it is tested as intended rather than quietly narrowed. Every other chairperson is
  refused, including a former Sunshine chairperson whose membership has ended. The sidebar link
  calls the same predicate, so the link and the 403 cannot drift apart.

Ruling: R41 — the dashboard panel deliberately writes NO AccessLog entry, unlike the full report.
  The callback runs on every load of the admin index, so logging it would put a row in the audit
  log each time anyone opens AEGIS at all. Spec 7.6's rule against one entry per row exists because
  "logging every row would bury the signal the log exists to provide", and an entry per index load
  buries it the same way. The panel shows the least sensitive data in the system — a name someone
  goes by and a day and month D17 already judged safe — and the full page, where anyone actually
  works from this data, is logged. The panel reuses the same access gate, so a Treasurer sees
  nothing; without that the 403 on the full page would be decoration, and there is a test per role
  proving it. Cost if wrong: someone reading only the dashboard panel leaves no audit trail.

Ruling: R42 — `aeci/templates/admin/index.html` shadows Unfold 0.104.1's own index to place the
  panel above the app list, reproducing its two helper includes because a template cannot extend
  one of the same name. That is an upgrade hazard: if Unfold's index grows a third include, this
  file silently loses it. Guarded by a test asserting the app list still renders, rather than by a
  comment nobody would read at upgrade time.
  Found and fixed while building it: Django's `{# #}` comment is single-line ONLY. A multi-line one
  is not recognised as a comment token at all — it either renders as literal text on the page (both
  of these templates did, verified live) or, if it contains a tag, is parsed as real template
  syntax (`'block' tag with name 'content' appears more than once`). Converted to
  `{% comment %}` blocks, with a test asserting no `{#` survives into either rendered page.

Residual (reported, not fixed): the weekly email digest spec 7.1 also describes is NOT built. It
  needs an email backend AECI has not configured and a schedule, and DEPLOYMENT.md 10 is honest
  that free-tier hosting has no usable cron — it would ride the same GitHub Actions workflow that
  still needs creating for backups. Deliberately deferred as infrastructure work rather than
  celebrations work, so nothing today tells the Secretary the list is worth looking at; they have
  to remember to open it, or notice the dashboard panel.
